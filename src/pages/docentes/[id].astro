---
import Layout from "../../layouts/Layout.astro";
import IconList from "../../components/faculty/IconList.astro";
import {
    getFacultyMembers,
    type FacultyEntry,
} from "../../lib/faculty";

export async function getStaticPaths() {
    const members = await getFacultyMembers();
    return members.map((member) => ({
        params: { id: member.slug },
        props: { member },
    }));
}

interface Props {
    member: FacultyEntry;
}

const { member } = Astro.props;
const { Content } = await member.render();

const {
    name,
    degree,
    bio,
    fullBio,
    position_laboral,
    education,
    experience_institutional,
    image,
} = member.data;

// Prefer the markdown body when authors moved the long bio there; fall back to
// the legacy `fullBio` frontmatter field, then to the short `bio`.
const hasMarkdownBody = (member.body ?? "").trim().length > 0;

const seo = {
    title: `${name} | Docentes CEPRIJA`,
    description: fullBio || bio,
    image,
};
---

<Layout seo={seo}>
    <main class="py-20 bg-gray-50">
        <div class="container mx-auto px-6">
            <div
                class="max-w-4xl mx-auto bg-white rounded-2xl shadow-lg overflow-hidden"
            >
                <div
                    class="md:grid md:grid-cols-12 md:items-stretch md:gap-0"
                >
                    <div
                        class="relative h-96 w-full overflow-hidden border-b border-gray-100 md:col-span-4 md:h-full md:min-h-0 md:border-b-0 md:border-r"
                        data-animate="zoom-in"
                        data-delay="100"
                    >
                        <img
                            src={image}
                            alt={name}
                            class="h-full w-full object-cover object-top"
                        />
                    </div>
                    <div class="md:col-span-8 p-8 md:p-12">
                        <h1
                            class="text-3xl md:text-4xl font-bold text-primary mb-2 font-serif"
                            data-animate="fade-up"
                        >
                            {name}
                        </h1>
                        <p
                            class="text-xl text-secondary font-medium mb-6"
                            data-animate="fade-up"
                            data-delay="100"
                        >
                            {degree}
                        </p>

                        <div
                            class="mb-8"
                            data-animate="fade-up"
                            data-delay="200"
                        >
                            <h2
                                class="text-lg font-bold text-gray-800 mb-3 border-b pb-2 font-serif"
                            >
                                Biografía
                            </h2>
                            {
                                hasMarkdownBody ? (
                                    <div class="prose prose-lg max-w-none text-gray-600 leading-relaxed">
                                        <Content />
                                    </div>
                                ) : (
                                    <p class="text-gray-600 leading-relaxed text-lg">
                                        {fullBio || bio}
                                    </p>
                                )
                            }
                        </div>

                        {
                            position_laboral && (
                                <div data-animate="fade-up" data-delay="200">
                                    <h2 class="text-lg font-bold text-gray-800 mb-3 border-b pb-2 font-serif">
                                        Cargo
                                    </h2>
                                    <p class="text-gray-600 leading-relaxed text-lg mb-8">
                                        {position_laboral}
                                    </p>
                                </div>
                            )
                        }

                        {
                            education && education.length > 0 && (
                                <IconList
                                    title="Formación Académica"
                                    items={education}
                                />
                            )
                        }

                        {
                            experience_institutional &&
                                experience_institutional.length > 0 && (
                                    <IconList
                                        title="Docencia en"
                                        items={experience_institutional}
                                    />
                                )
                        }

                        <div
                            class="mt-10 pt-6 border-t border-gray-100"
                            data-animate="fade-up"
                            data-delay="400"
                        >
                            <a
                                href="/docentes"
                                class="inline-flex items-center text-primary font-medium hover:text-secondary transition-colors"
                            >
                                <svg
                                    xmlns="http://www.w3.org/2000/svg"
                                    class="h-5 w-5 mr-2"
                                    fill="none"
                                    viewBox="0 0 24 24"
                                    stroke="currentColor"
                                    aria-hidden="true"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="2"
                                        d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                                </svg>
                                Volver a Docentes
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
</Layout>
