---
import { readdir } from "node:fs/promises";
import { extname, join, relative, sep } from "node:path";
import Layout from "../../layouts/Layout.astro";
import { getCollection } from "astro:content";
import ContactForm from "@components/forms/ContactForm.astro";
import ContinuousEducationForm from "@components/forms/ContinuousEducationForm.astro";
import ProgramEnrollmentAsideCard from "@components/program/ProgramEnrollmentAsideCard.astro";
import { getProgramPathSlug, validateUniqueSlugs } from "@lib/programPaths";
import { getEnrollmentFlow } from "@lib/enrollmentRouting";
import { programMetaDescriptionSuffix } from "../../data/config/seo.js";
import { filterPublishedPrograms } from "@lib/programPublished";
import {
    getPaymentOptions,
    getStripePriceIds,
    getDisplayPrices,
    getProgramStatus,
} from "@lib/programPayments";
import { getVariantOptions } from "@lib/programVariants";

export async function getStaticPaths() {
    const programs = await getCollection("programas");
    validateUniqueSlugs(programs);
    return filterPublishedPrograms(programs).map((program) => ({
        params: { slug: getProgramPathSlug(program) },
        props: { program },
    }));
}

const { program } = Astro.props;

const programStatus = getProgramStatus(program);
const enrollmentFlow = getEnrollmentFlow(program);
const programSlug = getProgramPathSlug(program);

/** Narrow optional arrays/objects so Astro/TS templates stay type-safe when schema inference is loose. */
type CurriculumPeriod = {
    period: string;
    subjects: string[];
    description?: string;
    professors?: string[];
};
type ProgramGalleryItem = {
    src: string;
    alt?: string;
    caption?: string;
};
const galleryImageExtensions = new Set([
    ".avif",
    ".gif",
    ".jpeg",
    ".jpg",
    ".png",
    ".svg",
    ".webp",
]);
const publicDirectory = join(process.cwd(), "public");

function normalizePublicFolderPath(folderPath: unknown): string | null {
    if (typeof folderPath !== "string") return null;
    const normalized = folderPath.trim().replace(/^\/+/, "");
    if (!normalized || normalized.includes("..")) return null;
    return normalized;
}

async function getGalleryFolderItems(
    folderPath: unknown,
    programTitle: string,
): Promise<ProgramGalleryItem[]> {
    const normalizedFolder = normalizePublicFolderPath(folderPath);
    if (!normalizedFolder) return [];

    const absoluteFolder = join(publicDirectory, normalizedFolder);
    const relativeFolder = relative(publicDirectory, absoluteFolder);
    if (
        relativeFolder.startsWith("..") ||
        relativeFolder.split(sep).includes("..")
    ) {
        return [];
    }

    try {
        const entries = await readdir(absoluteFolder, { withFileTypes: true });
        return entries
            .filter(
                (entry) =>
                    entry.isFile() &&
                    galleryImageExtensions.has(extname(entry.name).toLowerCase()),
            )
            .sort((a, b) =>
                a.name.localeCompare(b.name, "es", { sensitivity: "base" }),
            )
            .map((entry, index) => ({
                src: `/${normalizedFolder}/${entry.name}`.replace(/\/{2,}/g, "/"),
                alt: `${programTitle} - imagen ${index + 1}`,
            }));
    } catch {
        return [];
    }
}

const data = program.data;
const curriculumItems: CurriculumPeriod[] = Array.isArray(data.curriculum)
    ? (data.curriculum as CurriculumPeriod[])
    : [];
const explicitGalleryItems: ProgramGalleryItem[] = Array.isArray(data.gallery)
    ? (data.gallery as ProgramGalleryItem[])
    : [];
const folderGalleryItems = await getGalleryFolderItems(
    data.galleryFolder,
    String(data.title),
);
const galleryItems: ProgramGalleryItem[] = [
    ...explicitGalleryItems,
    ...folderGalleryItems,
];
const includesItems: string[] = Array.isArray(data.includes)
    ? (data.includes as string[])
    : [];
const prerequisitesItems: string[] = Array.isArray(data.prerequisites)
    ? (data.prerequisites as string[])
    : [];
// Extract payment data using new helper functions (with backward compatibility)
const paymentOptions = getPaymentOptions(program);
const variantOptions = getVariantOptions(program);
const stripePriceIdsForCheckout = getStripePriceIds(program);
const continuousEducationPrices = getDisplayPrices(program);
const programAddress =
    typeof data.address === "string" ? data.address : undefined;
const programBrochure =
    typeof data.brochure === "string" && data.brochure.trim().length > 0
        ? data.brochure.trim()
        : "";
const programRequiresVerification = Boolean(data.requiresVerification);
const registroAcademico =
    typeof data.registroAcademico === "string"
        ? data.registroAcademico.trim()
        : "";

const isDiplomado = data.nivel === "diplomado";
const descriptionPlain = String(data.description ?? "").trim();
const seo = {
    title: `${String(data.title)} | CEPRIJA`,
    description:
        descriptionPlain.length > 0
            ? `${descriptionPlain}${programMetaDescriptionSuffix}`
            : descriptionPlain,
    image: String(data.image || "/images/branding/site/ceprija_02.webp"),
};
/** Encuadre del hero con cover; sin `imagePosition` equivale al comportamiento por defecto del navegador (`center center`). */
const heroObjectPosition =
    typeof data.imagePosition === "string" &&
    data.imagePosition.trim().length > 0
        ? data.imagePosition.trim()
        : "center center";
---

<Layout seo={seo}>
    <main class="bg-gray-50 min-h-screen">
        <!-- Hero Section -->
        <section
            class="relative text-white py-20 min-h-[400px] flex items-center"
        >
            <div class="absolute inset-0 z-0">
                <img
                    src={String(data.image)}
                    alt={String(data.title)}
                    class="w-full h-full object-cover"
                    style={`object-position: ${heroObjectPosition}`}
                />
            </div>
            <!-- Gradiente: más opaco arriba (zona de título) para sostener texto blanco sobre fotos claras. -->
            <div
                class="absolute inset-0 z-0 pointer-events-none bg-gradient-to-b from-blue-950/90 via-blue-950/72 to-blue-950/50"
                aria-hidden="true"
            />
            <div class="container mx-auto px-6 relative z-10">
                <div class="max-w-4xl">
                    <span
                        class="bg-black/35 ring-1 ring-white/25 backdrop-blur-sm text-white text-sm font-bold px-4 py-2 rounded-full uppercase tracking-wide inline-block mb-4 shadow-sm"
                        data-animate="fade-up"
                    >
                        {program.data.nivel}
                    </span>
                    <h1
                        class="text-4xl font-bold mb-6 font-serif text-white drop-shadow-[0_2px_16px_rgba(0,0,0,0.45)]"
                        data-animate="fade-up"
                        data-delay="100"
                    >
                        {program.data.title}
                    </h1>
                    <p
                        class="text-xl text-gray-100 mb-8 drop-shadow-[0_1px_8px_rgba(0,0,0,0.4)]"
                        data-animate="fade-up"
                        data-delay="200"
                    >
                        {program.data.description}
                    </p>

                    <div
                        class="flex flex-wrap gap-6 text-sm drop-shadow-[0_1px_8px_rgba(0,0,0,0.35)]"
                        data-animate="fade-up"
                        data-delay="300"
                    >
                        {
                            program.data.rvoe && (
                                <div class="flex items-center gap-2">
                                    <span>RVOE: </span>
                                    <span class="italic font-bold">
                                        {program.data.rvoe}
                                    </span>
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-5 w-5"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                                        />
                                    </svg>
                                </div>
                            )
                        }

                        {
                            registroAcademico && (
                                <div class="flex items-center gap-2">
                                    <span>Registro: </span>
                                    <span class="italic font-bold">
                                        {registroAcademico}
                                    </span>
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-5 w-5"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                                        />
                                    </svg>
                                </div>
                            )
                        }

                        {
                            program.data.startDate && (
                                <div class="flex items-center gap-2">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-5 w-5"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
                                        />
                                    </svg>
                                    <span class="font-bold">
                                        {program.data.startDate}
                                    </span>
                                </div>
                            )
                        }

                        <div class="flex items-center gap-2">
                            <svg
                                xmlns="http://www.w3.org/2000/svg"
                                class="h-5 w-5"
                                fill="none"
                                viewBox="0 0 24 24"
                                stroke="currentColor"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                                ></path>
                            </svg>
                            <span>{program.data.duracion}</span>
                        </div>

                        {
                            program.data.horario && (
                                <div class="flex items-center gap-2">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-5 w-5"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                                        />
                                    </svg>
                                    <span>{program.data.horario}</span>
                                </div>
                            )
                        }

                        <div class="flex items-center gap-2">
                            <svg
                                xmlns="http://www.w3.org/2000/svg"
                                class="h-5 w-5"
                                fill="none"
                                viewBox="0 0 24 24"
                                stroke="currentColor"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                                ></path>
                            </svg>
                            <span>{program.data.modalidad}</span>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        {
            programStatus !== "waitlist" && (
                <!-- Content Section: información del programa a ancho legible -->
                <section class="py-16">
                    <div class="container mx-auto px-6 max-w-4xl">
                        <div class="space-y-12">
                    <!-- Plan de Estudios -->
                    {/* 
                    {
                        curriculumItems.length > 0 && (
                            <div
                                class="bg-white rounded-2xl shadow-sm p-8"
                                data-animate="fade-up"
                                data-delay="100"
                            >
                                <h2 class="text-3xl font-bold text-primary mb-6 font-serif flex items-center gap-3">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-8 w-8 text-secondary"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"
                                        />
                                    </svg>
                                    {program.data.curriculumTitle ||
                                        "Plan de Estudios"}
                                </h2>
                                <div class="space-y-4">
                                    {curriculumItems.map(
                                        (semester, modIndex) => {
                                            const profs = Array.isArray(
                                                semester.professors,
                                            )
                                                ? semester.professors.filter(
                                                      (p) =>
                                                          typeof p ===
                                                              "string" &&
                                                          p.trim(),
                                                  )
                                                : [];
                                            return (
                                                <details
                                                    class="group border border-gray-200 rounded-xl bg-gray-50/80 shadow-sm overflow-hidden open:shadow-md transition-shadow"
                                                    open={modIndex === 0}
                                                >
                                                    <summary class="cursor-pointer list-none flex items-center justify-between gap-4 px-5 py-4 font-serif text-lg md:text-xl font-bold text-primary hover:bg-white/80 transition-colors [&::-webkit-details-marker]:hidden">
                                                        <span class="text-left">
                                                            {semester.period}
                                                        </span>
                                                        <svg
                                                            xmlns="http://www.w3.org/2000/svg"
                                                            class="h-6 w-6 text-secondary shrink-0 transition-transform duration-200 group-open:rotate-180"
                                                            fill="none"
                                                            viewBox="0 0 24 24"
                                                            stroke="currentColor"
                                                            aria-hidden="true"
                                                        >
                                                            <path
                                                                stroke-linecap="round"
                                                                stroke-linejoin="round"
                                                                stroke-width="2"
                                                                d="M19 9l-7 7-7-7"
                                                            />
                                                        </svg>
                                                    </summary>
                                                    <div class="px-5 pb-5 pt-0 border-t border-gray-200/80 bg-white space-y-5">
                                                        {semester.description?.trim() && (
                                                            <p class="text-gray-700 leading-relaxed text-base pt-4">
                                                                {semester.description.trim()}
                                                            </p>
                                                        )}
                                                        {profs.length > 0 && (
                                                            <div>
                                                                <h4 class="text-xs font-bold text-primary uppercase tracking-wider mb-2">
                                                                    Docentes
                                                                </h4>
                                                                <ul class="space-y-1.5 text-gray-700">
                                                                    {profs.map(
                                                                        (p) => (
                                                                            <li class="flex items-start gap-2">
                                                                                <span
                                                                                    class="mt-1.5 h-1.5 w-1.5 rounded-full bg-secondary shrink-0"
                                                                                    aria-hidden="true"
                                                                                />
                                                                                <span>
                                                                                    {p.trim()}
                                                                                </span>
                                                                            </li>
                                                                        ),
                                                                    )}
                                                                </ul>
                                                            </div>
                                                        )}
                                                        <div>
                                                            <h4 class="text-xs font-bold text-primary uppercase tracking-wider mb-3">
                                                                Temas
                                                            </h4>
                                                            <div class="grid md:grid-cols-2 gap-3">
                                                                {semester.subjects.map(
                                                                    (
                                                                        subject,
                                                                        subIndex,
                                                                    ) => (
                                                                        <div class="flex items-start gap-3 p-3 bg-gray-50 rounded-lg border border-gray-100 hover:border-secondary/30 hover:bg-blue-50/50 transition-colors">
                                                                            <span class="flex-shrink-0 w-6 h-6 bg-secondary text-primary rounded-full flex items-center justify-center text-xs font-bold">
                                                                                {subIndex +
                                                                                    1}
                                                                            </span>
                                                                            <span class="flex flex-col gap-1.5 text-sm md:text-base leading-snug">
                                                                                {subject.includes(
                                                                                    "\n",
                                                                                ) ? (
                                                                                    subject
                                                                                        .split(
                                                                                            "\n",
                                                                                        )
                                                                                        .map(
                                                                                            (
                                                                                                line,
                                                                                            ) =>
                                                                                                line.trim(),
                                                                                        )
                                                                                        .filter(
                                                                                            Boolean,
                                                                                        )
                                                                                        .map(
                                                                                            (
                                                                                                line,
                                                                                                lineIdx,
                                                                                            ) => (
                                                                                                <span
                                                                                                    class:list={[
                                                                                                        lineIdx ===
                                                                                                        0
                                                                                                            ? "font-bold text-primary"
                                                                                                            : "text-gray-700 font-medium",
                                                                                                    ]}
                                                                                                >
                                                                                                    {
                                                                                                        line
                                                                                                    }
                                                                                                </span>
                                                                                            ),
                                                                                        )
                                                                                ) : (
                                                                                    <span class="text-gray-700 font-medium">
                                                                                        {
                                                                                            subject
                                                                                        }
                                                                                    </span>
                                                                                )}
                                                                            </span>
                                                                        </div>
                                                                    ),
                                                                )}
                                                            </div>
                                                        </div>
                                                    </div>
                                                </details>
                                            );
                                        },
                                    )}
                                </div>
                            </div>
                        )
                    }
                    */}
                    {
                        galleryItems.length > 0 && (
                            <div
                                class="bg-white rounded-2xl shadow-sm p-8"
                                data-animate="fade-up"
                                data-delay="150"
                            >
                                <h2 class="text-3xl font-bold text-primary mb-6 font-serif flex items-center gap-3">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-8 w-8 text-secondary"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"
                                        />
                                    </svg>
                                    Temario
                                </h2>

                                <div
                                    class="relative overflow-hidden rounded-2xl border border-gray-100 bg-gray-100"
                                    data-program-gallery-carousel
                                >
                                    <div
                                        class="flex transition-transform duration-500 ease-in-out"
                                        data-program-gallery-track
                                    >
                                        {galleryItems.map((item) => (
                                            <figure class="relative min-w-full h-[360px] sm:h-[460px] lg:h-[540px] bg-gray-100">
                                                <img
                                                    src={item.src}
                                                    alt={item.alt ?? ""}
                                                    class="h-full w-full object-contain"
                                                    loading="lazy"
                                                    decoding="async"
                                                />

                                                {item.caption ? (
                                                    <figcaption class="absolute bottom-0 left-0 right-0 bg-black/55 px-4 py-3 text-sm text-white">
                                                        {item.caption}
                                                    </figcaption>
                                                ) : null}
                                            </figure>
                                        ))}
                                    </div>

                                    {galleryItems.length > 1 ? (
                                        <>
                                            <button
                                                type="button"
                                                class="absolute left-4 top-1/2 flex h-11 w-11 -translate-y-1/2 items-center justify-center rounded-full bg-white/85 text-primary shadow-md transition hover:bg-white focus:outline-none focus-visible:ring-2 focus-visible:ring-secondary"
                                                data-program-gallery-prev
                                                aria-label="Imagen anterior"
                                            >
                                                <svg
                                                    xmlns="http://www.w3.org/2000/svg"
                                                    class="h-6 w-6"
                                                    fill="none"
                                                    viewBox="0 0 24 24"
                                                    stroke="currentColor"
                                                    aria-hidden="true"
                                                >
                                                    <path
                                                        stroke-linecap="round"
                                                        stroke-linejoin="round"
                                                        stroke-width="2"
                                                        d="M15 19l-7-7 7-7"
                                                    />
                                                </svg>
                                            </button>

                                            <button
                                                type="button"
                                                class="absolute right-4 top-1/2 flex h-11 w-11 -translate-y-1/2 items-center justify-center rounded-full bg-white/85 text-primary shadow-md transition hover:bg-white focus:outline-none focus-visible:ring-2 focus-visible:ring-secondary"
                                                data-program-gallery-next
                                                aria-label="Imagen siguiente"
                                            >
                                                <svg
                                                    xmlns="http://www.w3.org/2000/svg"
                                                    class="h-6 w-6"
                                                    fill="none"
                                                    viewBox="0 0 24 24"
                                                    stroke="currentColor"
                                                    aria-hidden="true"
                                                >
                                                    <path
                                                        stroke-linecap="round"
                                                        stroke-linejoin="round"
                                                        stroke-width="2"
                                                        d="M9 5l7 7-7 7"
                                                    />
                                                </svg>
                                            </button>

                                            <div
                                                class="absolute bottom-4 left-1/2 flex -translate-x-1/2 gap-2"
                                                data-program-gallery-dots
                                            >
                                                {galleryItems.map((_, index) => (
                                                    <button
                                                        type="button"
                                                        class:list={[
                                                            "h-2.5 w-2.5 rounded-full transition focus:outline-none focus-visible:ring-2 focus-visible:ring-secondary",
                                                            index === 0
                                                                ? "bg-white"
                                                                : "bg-white/50",
                                                        ]}
                                                        data-program-gallery-dot={index}
                                                        aria-label={`Ver imagen ${index + 1}`}
                                                        aria-current={
                                                            index === 0
                                                                ? "true"
                                                                : "false"
                                                        }
                                                    />
                                                ))}
                                            </div>
                                        </>
                                    ) : null}
                                </div>
                            </div>
                        )
                    }
                    <!-- Perfil del Egresado -->
                    {
                        program.data.profile && (
                            <div
                                class="bg-white rounded-2xl shadow-sm p-8"
                                data-animate="fade-up"
                                data-delay="200"
                            >
                                <h2 class="text-3xl font-bold text-primary mb-6 font-serif flex items-center gap-3">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-8 w-8 text-secondary"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                                        />
                                    </svg>
                                    Perfil del Egresado
                                </h2>
                                <p class="text-gray-700 leading-relaxed text-lg">
                                    {program.data.profile}
                                </p>
                            </div>
                        )
                    }
                    <!-- Dirigido a -->
                    {
                        program.data.profileAudience && (
                            <div
                                class="bg-white rounded-2xl shadow-sm p-8"
                                data-animate="fade-up"
                                data-delay="200"
                            >
                                <h2 class="text-3xl font-bold text-primary mb-6 font-serif flex items-center gap-3">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-8 w-8 text-secondary"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                                        />
                                    </svg>
                                    Dirigido a
                                </h2>
                                <p class="text-gray-700 leading-relaxed text-lg">
                                    {program.data.profileAudience}
                                </p>
                            </div>
                        )
                    }

                    <!-- Campo Laboral -->
                    {
                        program.data.fieldOfWork && (
                            <div
                                class="bg-white rounded-2xl shadow-sm p-8"
                                data-animate="fade-up"
                                data-delay="300"
                            >
                                <h2 class="text-3xl font-bold text-primary mb-6 font-serif flex items-center gap-3">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-8 w-8 text-secondary"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M21 13.255A23.931 23.931 0 0112 15c-3.183 0-6.22-.62-9-1.745M16 6V4a2 2 0 00-2-2h-4a2 2 0 00-2 2v2m4 6h.01M5 20h14a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                                        />
                                    </svg>
                                    Campo Laboral
                                </h2>
                                <p class="text-gray-700 leading-relaxed text-lg">
                                    {program.data.fieldOfWork}
                                </p>
                            </div>
                        )
                    }

                    {
                        programBrochure && (
                            <div
                                class="bg-white rounded-2xl shadow-sm p-8"
                                data-animate="fade-up"
                                data-delay="400"
                            >
                                <h2 class="text-3xl font-bold text-primary mb-6 font-serif flex items-center gap-3">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-8 w-8 text-secondary"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                                        />
                                    </svg>
                                    Brochure del programa
                                </h2>
                                <p class="text-gray-700 leading-relaxed mb-6">
                                    Descarga el brochure para consultar la
                                    información completa del programa,
                                    incluyendo temario, inversión y detalles de
                                    inscripción.
                                </p>
                                <button
                                    type="button"
                                    data-brochure-open
                                    data-brochure-url={programBrochure}
                                    class="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-6 py-3 font-bold text-white shadow-lg transition-colors hover:bg-blue-900"
                                >
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-5 w-5"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414A1 1 0 0119 9.414V19a2 2 0 01-2 2z"
                                        />
                                    </svg>
                                    Descargar brochure
                                </button>
                            </div>
                        )
                    }

                    {
                        includesItems.length > 0 && (
                            <div
                                class="bg-white rounded-2xl shadow-sm p-8"
                                data-animate="fade-up"
                                data-delay="500"
                            >
                                <h2 class="text-3xl font-bold text-primary mb-6 font-serif flex items-center gap-3">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-8 w-8 text-secondary"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
                                        />
                                    </svg>
                                    Incluye
                                </h2>
                                <ul class="space-y-2">
                                    {includesItems.map((item) => (
                                        <li class="flex items-center gap-2 text-gray-700">
                                            <svg
                                                xmlns="http://www.w3.org/2000/svg"
                                                class="h-5 w-5 text-secondary"
                                                viewBox="0 0 20 20"
                                                fill="currentColor"
                                            >
                                                <path
                                                    fill-rule="evenodd"
                                                    d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                                                    clip-rule="evenodd"
                                                />
                                            </svg>
                                            {item}
                                        </li>
                                    ))}
                                </ul>
                            </div>
                        )
                    }

                    {
                        prerequisitesItems.length > 0 && (
                            <div
                                class="bg-white rounded-2xl shadow-sm p-8"
                                data-animate="fade-up"
                                data-delay="550"
                            >
                                <h2 class="text-3xl font-bold text-primary mb-6 font-serif flex items-center gap-3">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-8 w-8 text-secondary"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4"
                                        />
                                    </svg>
                                    Requisitos previos
                                </h2>
                                <ul class="space-y-2">
                                    {prerequisitesItems.map((item) => (
                                        <li class="flex items-center gap-2 text-gray-700">
                                            <svg
                                                xmlns="http://www.w3.org/2000/svg"
                                                class="h-5 w-5 text-secondary"
                                                viewBox="0 0 20 20"
                                                fill="currentColor"
                                            >
                                                <path
                                                    fill-rule="evenodd"
                                                    d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                                                    clip-rule="evenodd"
                                                />
                                            </svg>
                                            {item}
                                        </li>
                                    ))}
                                </ul>
                            </div>
                        )
                    }
                        </div>
                    </div>
                </section>
            )
        }

        <!-- Inscripción / admisión: sin data-animate aquí — el transform del fade-up
             hace que position:fixed de descendientes se ancle al section y el pie
             del formulario desaparezca. `enrollmentFlow: inline` en el .md sigue
             gobernado por getEnrollmentFlow(). -->
        <section class="py-16 pb-20 border-t border-gray-200 bg-gray-50/80">
            <div class="container mx-auto px-6 max-w-4xl lg:max-w-5xl">
                {
                    programStatus === "waitlist" ? (
                        <div class="max-w-4xl mx-auto">
                            <div class="bg-white rounded-3xl shadow-xl p-8 md:p-12">
                                <div class="text-center mb-8">
                                    <div class="inline-block p-4 bg-amber-50 rounded-2xl mb-4">
                                        <svg
                                            xmlns="http://www.w3.org/2000/svg"
                                            class="h-12 w-12 text-amber-500"
                                            fill="none"
                                            viewBox="0 0 24 24"
                                            stroke="currentColor"
                                        >
                                            <path
                                                stroke-linecap="round"
                                                stroke-linejoin="round"
                                                stroke-width="2"
                                                d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                                            />
                                        </svg>
                                    </div>
                                    <h3 class="text-2xl md:text-3xl font-bold text-primary mb-4 font-serif">
                                        Próximamente Disponible
                                    </h3>
                                    <p class="text-gray-600 text-lg leading-relaxed max-w-2xl mx-auto">
                                        Este programa no está actualmente en calendario.
                                        Déjanos tus datos y te contactaremos cuando abramos inscripciones.
                                    </p>
                                </div>
                                <ContactForm programTitle={String(data.title)} />
                            </div>
                        </div>
                    ) : enrollmentFlow === "application" ? (
                        <ProgramEnrollmentAsideCard
                            sticky={false}
                            class="shadow-xl"
                        >
                            <h3 class="text-2xl font-bold text-primary mb-2 font-serif">
                                {isDiplomado
                                    ? "Registro al Diplomado"
                                    : "Proceso de Admisión"}
                            </h3>
                            <p class="text-gray-600 mb-6 text-sm">
                                {isDiplomado
                                    ? "Completa tu registro con documentación académica y después elige tu forma de pago."
                                    : "Este programa requiere un proceso formal de admisión con documentación académica."}
                            </p>

                            <div class="space-y-3 mb-6 text-sm">
                                <div class="flex items-start gap-2">
                                    <svg
                                        class="w-5 h-5 text-secondary mt-0.5 flex-shrink-0"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
                                        />
                                    </svg>
                                    <span class="text-gray-700">
                                        Curriculum Vitae
                                    </span>
                                </div>
                                <div class="flex items-start gap-2">
                                    <svg
                                        class="w-5 h-5 text-secondary mt-0.5 flex-shrink-0"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"
                                        />
                                    </svg>
                                    <span class="text-gray-700">
                                        Título Profesional y Cédula por cada
                                        grado académico
                                    </span>
                                </div>
                            </div>

                            <a
                                href={`/enrollment/${programSlug}`}
                                class="block w-full bg-secondary hover:bg-secondary/90 text-white font-bold py-4 px-6 rounded-xl text-center transition-all shadow-lg hover:shadow-xl"
                            >
                                {isDiplomado
                                    ? "Iniciar Registro"
                                    : "Iniciar Solicitud"}
                            </a>

                            <p class="text-xs text-gray-500 mt-4 text-center">
                                {isDiplomado
                                    ? "Registro en línea • Pago al finalizar"
                                    : "Proceso en línea • Respuesta en 48-72 horas"}
                            </p>
                        </ProgramEnrollmentAsideCard>
                    ) : (
                        <div class="max-w-3xl mx-auto">
                            <div class="bg-white rounded-3xl shadow-xl p-8 md:p-12 text-center">
                                <div class="inline-flex h-16 w-16 items-center justify-center rounded-2xl bg-secondary/15 text-secondary mb-5">
                                    <svg
                                        xmlns="http://www.w3.org/2000/svg"
                                        class="h-9 w-9"
                                        fill="none"
                                        viewBox="0 0 24 24"
                                        stroke="currentColor"
                                        aria-hidden="true"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414A1 1 0 0119 9.414V19a2 2 0 01-2 2z"
                                        />
                                    </svg>
                                </div>
                                <h3 class="text-2xl md:text-3xl font-bold text-primary mb-4 font-serif">
                                    Inscripción al programa
                                </h3>
                                <p class="text-gray-600 text-lg leading-relaxed max-w-2xl mx-auto mb-8">
                                    Completa tu registro en línea y selecciona
                                    tu modalidad o forma de pago disponible.
                                </p>
                                <button
                                    type="button"
                                    data-enrollment-modal-open
                                    class="inline-flex items-center justify-center gap-2 rounded-xl bg-secondary px-8 py-4 font-bold text-primary shadow-lg transition-colors hover:bg-secondary/90 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary"
                                >
                                    Inscríbete Ya
                                </button>
                            </div>
                        </div>
                    )
                }
            </div>
        </section>
        {
            enrollmentFlow === "inline" && programStatus !== "waitlist" && (
                <div
                    id="enrollment-modal"
                    class="fixed inset-0 z-50 flex pointer-events-none items-center justify-center bg-black/60 px-4 py-8 opacity-0 transition-opacity duration-200 ease-out"
                    aria-hidden="true"
                    inert
                    role="dialog"
                    aria-modal="true"
                    aria-labelledby="enrollment-modal-title"
                    aria-describedby="enrollment-modal-description"
                >
                    <div
                        class="relative w-full max-w-5xl max-h-[90vh] scale-95 overflow-y-auto rounded-3xl bg-white p-6 opacity-0 shadow-2xl transition-all duration-200 ease-out md:p-8"
                        data-modal-panel
                    >
                        <button
                            type="button"
                            id="enrollment-modal-close"
                            class="absolute right-4 top-4 z-10 rounded-full p-2 text-gray-500 transition-colors hover:bg-gray-100 hover:text-gray-900"
                            aria-label="Cerrar"
                        >
                            <svg
                                xmlns="http://www.w3.org/2000/svg"
                                class="h-5 w-5"
                                fill="none"
                                viewBox="0 0 24 24"
                                stroke="currentColor"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M6 18L18 6M6 6l12 12"
                                />
                            </svg>
                        </button>

                        <div class="mb-8 pr-10">
                            <h2 id="enrollment-modal-title" class="mb-2 text-2xl md:text-3xl font-bold text-primary font-serif">
                                Inscríbete Ya
                            </h2>
                            <p id="enrollment-modal-description" class="text-sm md:text-base text-gray-600">
                                Completa el formulario para registrar tu interés
                                y continuar con tu inscripción.
                            </p>
                        </div>

                        <ContinuousEducationForm
                            programTitle={String(data.title)}
                            programId={programSlug}
                            stripePriceIds={stripePriceIdsForCheckout}
                            prices={continuousEducationPrices}
                            paymentOptions={paymentOptions}
                            variantOptions={variantOptions}
                            modalidad={String(data.modalidad)}
                            address={programAddress}
                            requiresVerification={programRequiresVerification}
                            embedInAsideCard={false}
                        />
                    </div>
                </div>
            )
        }
        {
            programBrochure && (
                <div
                    id="brochure-modal"
                    class="fixed inset-0 z-50 flex pointer-events-none items-center justify-center bg-black/60 px-4 py-8 opacity-0 transition-opacity duration-200 ease-out"
                    aria-hidden="true"
                    inert
                    role="dialog"
                    aria-modal="true"
                    aria-labelledby="brochure-modal-title"
                    aria-describedby="brochure-modal-description"
                >
                    <div
                        class="relative w-full max-w-xl scale-95 rounded-3xl bg-white p-6 opacity-0 shadow-2xl transition-all duration-200 ease-out md:p-8"
                        data-modal-panel
                    >
                        <button
                            type="button"
                            id="brochure-modal-close"
                            class="absolute right-4 top-4 rounded-full p-2 text-gray-500 transition-colors hover:bg-gray-100 hover:text-gray-900"
                            aria-label="Cerrar"
                        >
                            <svg
                                xmlns="http://www.w3.org/2000/svg"
                                class="h-5 w-5"
                                fill="none"
                                viewBox="0 0 24 24"
                                stroke="currentColor"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M6 18L18 6M6 6l12 12"
                                />
                            </svg>
                        </button>

                        <h2 id="brochure-modal-title" class="mb-2 pr-8 text-2xl font-bold text-primary font-serif">
                            Descarga el brochure
                        </h2>
                        <p id="brochure-modal-description" class="mb-6 text-sm text-gray-600">
                            Déjanos tus datos para enviarte seguimiento
                            personalizado. Al enviar el formulario se iniciará
                            la descarga del PDF.
                        </p>

                        <form
                            id="brochure-lead-form"
                            class="space-y-4"
                            data-program-title={String(data.title)}
                            data-program-slug={programSlug}
                            data-brochure={programBrochure}
                        >
                            <input
                                type="text"
                                name="website"
                                autocomplete="off"
                                tabindex="-1"
                                class="sr-only"
                                aria-hidden="true"
                            />
                            <div>
                                <label
                                    class="mb-2 block text-sm font-semibold text-gray-700"
                                    for="brochure-name"
                                >
                                    Nombre completo *
                                </label>
                                <input
                                    id="brochure-name"
                                    name="name"
                                    type="text"
                                    required
                                    class="w-full rounded-xl border-2 border-gray-100 px-4 py-3 outline-none transition-all focus:border-primary focus:ring-4 focus:ring-primary/5"
                                    placeholder="Tu nombre"
                                />
                            </div>
                            <div class="grid gap-4 md:grid-cols-2">
                                <div>
                                    <label
                                        class="mb-2 block text-sm font-semibold text-gray-700"
                                        for="brochure-email"
                                    >
                                        Correo electrónico *
                                    </label>
                                    <input
                                        id="brochure-email"
                                        name="email"
                                        type="email"
                                        required
                                        class="w-full rounded-xl border-2 border-gray-100 px-4 py-3 outline-none transition-all focus:border-primary focus:ring-4 focus:ring-primary/5"
                                        placeholder="tu@email.com"
                                    />
                                </div>
                                <div>
                                    <label
                                        class="mb-2 block text-sm font-semibold text-gray-700"
                                        for="brochure-phone"
                                    >
                                        Teléfono *
                                    </label>
                                    <input
                                        id="brochure-phone"
                                        name="phone"
                                        type="tel"
                                        required
                                        class="w-full rounded-xl border-2 border-gray-100 px-4 py-3 outline-none transition-all focus:border-primary focus:ring-4 focus:ring-primary/5"
                                        placeholder="33 0000 0000"
                                    />
                                </div>
                            </div>
                            <div>
                                <label
                                    class="mb-2 block text-sm font-semibold text-gray-700"
                                    for="brochure-message"
                                >
                                    Mensaje
                                </label>
                                <textarea
                                    id="brochure-message"
                                    name="message"
                                    rows="3"
                                    class="w-full rounded-xl border-2 border-gray-100 px-4 py-3 outline-none transition-all focus:border-primary focus:ring-4 focus:ring-primary/5"
                                    placeholder="Cuéntanos si tienes alguna duda"
                                ></textarea>
                            </div>
                            <p
                                id="brochure-form-status"
                                class="hidden rounded-xl px-4 py-3 text-sm"
                                role="status"
                            >
                            </p>
                            <button
                                type="submit"
                                id="brochure-submit"
                                class="w-full rounded-xl bg-secondary px-6 py-3 font-bold text-primary shadow-lg transition-colors hover:bg-secondary/90 disabled:cursor-not-allowed disabled:opacity-70"
                            >
                                Enviar y descargar
                            </button>
                        </form>
                    </div>
                </div>
            )
        }
    </main>

    <script>
        function downloadBrochure(url: string) {
            const link = document.createElement("a");
            link.href = url;
            link.download = "";
            document.body.appendChild(link);
            link.click();
            link.remove();
        }

        const brochureModal = document.getElementById("brochure-modal");
        const brochureOpenButtons =
            document.querySelectorAll<HTMLButtonElement>("[data-brochure-open]");
        const brochureCloseButton = document.getElementById(
            "brochure-modal-close",
        );
        const brochureForm = document.getElementById(
            "brochure-lead-form",
        ) as HTMLFormElement | null;
        const brochureStatus = document.getElementById("brochure-form-status");
        const brochureSubmit = document.getElementById(
            "brochure-submit",
        ) as HTMLButtonElement | null;
        let brochureLastFocused: HTMLElement | null = null;
        const enrollmentModal = document.getElementById("enrollment-modal");
        const enrollmentOpenButtons =
            document.querySelectorAll<HTMLButtonElement>("[data-enrollment-modal-open]");
        const enrollmentCloseButton = document.getElementById(
            "enrollment-modal-close",
        );
        let enrollmentLastFocused: HTMLElement | null = null;

        function focusableElements(container: Element): HTMLElement[] {
            return Array.from(
                container.querySelectorAll<HTMLElement>(
                    'a[href], button:not([disabled]), input:not([disabled]), textarea:not([disabled]), select:not([disabled]), [tabindex]:not([tabindex="-1"])',
                ),
            );
        }

        function isModalOpen(modal: HTMLElement | null): modal is HTMLElement {
            return !!modal && modal.getAttribute("aria-hidden") === "false";
        }

        function setModalVisualState(modal: HTMLElement, open: boolean) {
            const panel = modal.querySelector<HTMLElement>("[data-modal-panel]");
            modal.classList.toggle("opacity-0", !open);
            modal.classList.toggle("opacity-100", open);
            modal.classList.toggle("pointer-events-none", !open);
            modal.classList.toggle("pointer-events-auto", open);
            modal.setAttribute("aria-hidden", String(!open));
            if (open) {
                modal.removeAttribute("inert");
            } else {
                modal.setAttribute("inert", "");
            }
            panel?.classList.toggle("opacity-0", !open);
            panel?.classList.toggle("opacity-100", open);
            panel?.classList.toggle("scale-95", !open);
            panel?.classList.toggle("scale-100", open);
        }

        function setBrochureModalOpen(open: boolean) {
            if (!brochureModal) return;
            if (open) brochureLastFocused = document.activeElement as HTMLElement | null;
            setModalVisualState(brochureModal, open);
            document.body.classList.toggle("overflow-hidden", open);
            if (!open) brochureLastFocused?.focus?.();
        }

        function setEnrollmentModalOpen(open: boolean) {
            if (!enrollmentModal) return;
            if (open) enrollmentLastFocused = document.activeElement as HTMLElement | null;
            setModalVisualState(enrollmentModal, open);
            document.body.classList.toggle("overflow-hidden", open);
            if (open) {
                window.setTimeout(() => {
                    focusableElements(enrollmentModal)[0]?.focus();
                }, 0);
            } else {
                enrollmentLastFocused?.focus?.();
            }
        }

        function showBrochureStatus(kind: "error" | "success", message: string) {
            if (!brochureStatus) return;
            brochureStatus.textContent = message;
            brochureStatus.className =
                kind === "success"
                    ? "rounded-xl px-4 py-3 text-sm bg-green-50 text-green-800 border border-green-200"
                    : "rounded-xl px-4 py-3 text-sm bg-red-50 text-red-800 border border-red-200";
        }

        brochureOpenButtons.forEach((button) => {
            button.addEventListener("click", () => {
                setBrochureModalOpen(true);
                window.setTimeout(() => {
                    document.getElementById("brochure-name")?.focus();
                }, 0);
            });
        });

        brochureCloseButton?.addEventListener("click", () => {
            setBrochureModalOpen(false);
        });

        brochureModal?.addEventListener("click", (event) => {
            if (event.target === brochureModal) setBrochureModalOpen(false);
        });

        enrollmentOpenButtons.forEach((button) => {
            button.addEventListener("click", () => {
                setEnrollmentModalOpen(true);
            });
        });

        enrollmentCloseButton?.addEventListener("click", () => {
            setEnrollmentModalOpen(false);
        });

        enrollmentModal?.addEventListener("click", (event) => {
            if (event.target === enrollmentModal) setEnrollmentModalOpen(false);
        });

        window.addEventListener("keydown", (event) => {
            const activeModal =
                isModalOpen(enrollmentModal)
                    ? enrollmentModal
                    : isModalOpen(brochureModal)
                      ? brochureModal
                      : null;
            if (event.key === "Escape") {
                if (activeModal === enrollmentModal) setEnrollmentModalOpen(false);
                if (activeModal === brochureModal) setBrochureModalOpen(false);
                return;
            }
            if (event.key !== "Tab" || !activeModal) return;
            const focusable = focusableElements(activeModal);
            const first = focusable[0];
            const last = focusable[focusable.length - 1];
            if (!first || !last) return;
            if (event.shiftKey && document.activeElement === first) {
                event.preventDefault();
                last.focus();
            } else if (!event.shiftKey && document.activeElement === last) {
                event.preventDefault();
                first.focus();
            }
        });

        brochureForm?.addEventListener("submit", async (event) => {
            event.preventDefault();
            if (!brochureForm.reportValidity()) return;

            const formData = new FormData(brochureForm);
            const brochure = brochureForm.dataset.brochure ?? "";
            const trackingRequestId =
                (globalThis.crypto?.randomUUID?.() ??
                    `${Date.now()}-${Math.random().toString(16).slice(2)}`) as string;
            if (brochureSubmit) {
                brochureSubmit.disabled = true;
                brochureSubmit.textContent = "Enviando...";
            }

            try {
                const payload = {
                    trackingRequestId,
                    name: String(formData.get("name") ?? ""),
                    email: String(formData.get("email") ?? ""),
                    phone: String(formData.get("phone") ?? ""),
                    message: String(formData.get("message") ?? ""),
                    website: String(formData.get("website") ?? ""),
                    programTitle: brochureForm.dataset.programTitle ?? "",
                    programSlug: brochureForm.dataset.programSlug ?? "",
                    brochure,
                };

                const response = await fetch("/api/brochure-download", {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify(payload),
                });
                const data = await response.json().catch(() => ({}));
                if (!response.ok) {
                    throw new Error(
                        typeof data.error === "string"
                            ? data.error
                            : "No pudimos enviar tus datos. Intenta de nuevo.",
                    );
                }

                showBrochureStatus(
                    "success",
                    "Gracias. Iniciaremos la descarga del brochure.",
                );
                downloadBrochure(String(data.brochure || brochure));

                // If School Hub persistence timed out, retry in the background.
                if (data?.tracking?.ok === false) {
                    const retryDelaysMs = [500, 2000, 5000, 10000, 30000];
                    let stopped = false;

                    const attempt = async () => {
                        try {
                            const res = await fetch("/api/brochure-track", {
                                method: "POST",
                                headers: { "Content-Type": "application/json" },
                                body: JSON.stringify(payload),
                            });
                            const r = await res.json().catch(() => ({}));
                            return !!(res.ok && r && r.ok === true);
                        } catch {
                            return false;
                        }
                    };

                    const runRetries = async () => {
                        for (const delay of retryDelaysMs) {
                            if (stopped) return;
                            const ok = await attempt();
                            if (ok) return;
                            await new Promise((r) => window.setTimeout(r, delay));
                        }
                    };

                    void runRetries();

                    window.addEventListener(
                        "pagehide",
                        () => {
                            stopped = true;
                            try {
                                const blob = new Blob([JSON.stringify(payload)], {
                                    type: "application/json",
                                });
                                navigator.sendBeacon("/api/brochure-track", blob);
                            } catch {
                                // ignore
                            }
                        },
                        { once: true },
                    );
                }

                brochureForm.reset();
                window.setTimeout(() => setBrochureModalOpen(false), 900);
            } catch (error) {
                showBrochureStatus(
                    "error",
                    error instanceof Error
                        ? error.message
                        : "No pudimos enviar tus datos. Intenta de nuevo.",
                );
            } finally {
                if (brochureSubmit) {
                    brochureSubmit.disabled = false;
                    brochureSubmit.textContent = "Enviar y descargar";
                }
            }
        });

        document
            .querySelectorAll<HTMLElement>("[data-program-gallery-carousel]")
            .forEach((carousel) => {
                const track = carousel.querySelector<HTMLElement>(
                    "[data-program-gallery-track]",
                );
                const prevButton = carousel.querySelector<HTMLButtonElement>(
                    "[data-program-gallery-prev]",
                );
                const nextButton = carousel.querySelector<HTMLButtonElement>(
                    "[data-program-gallery-next]",
                );
                const dots = Array.from(
                    carousel.querySelectorAll<HTMLButtonElement>(
                        "[data-program-gallery-dot]",
                    ),
                );
                const totalSlides = track?.children.length ?? 0;
                let currentSlide = 0;

                if (!track || totalSlides <= 1) return;
                const galleryTrack = track;

                function showSlide(index: number) {
                    currentSlide = (index + totalSlides) % totalSlides;
                    galleryTrack.style.transform = `translateX(-${currentSlide * 100}%)`;

                    dots.forEach((dot, dotIndex) => {
                        const isActive = dotIndex === currentSlide;
                        dot.classList.toggle("bg-white", isActive);
                        dot.classList.toggle("bg-white/50", !isActive);
                        dot.setAttribute("aria-current", String(isActive));
                    });
                }

                prevButton?.addEventListener("click", () => {
                    showSlide(currentSlide - 1);
                });

                nextButton?.addEventListener("click", () => {
                    showSlide(currentSlide + 1);
                });

                dots.forEach((dot) => {
                    const index = Number(dot.dataset.programGalleryDot);
                    if (Number.isNaN(index)) return;
                    dot.addEventListener("click", () => {
                        showSlide(index);
                    });
                });
            });
    </script>
</Layout>
